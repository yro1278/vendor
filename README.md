
  # Vendor Management Module

  This is a code bundle for Vendor Management Module. The original project is available at https://www.figma.com/design/CYA1AygcMg2pNIHk3jQe9b/Vendor-Management-Module.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  